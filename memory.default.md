<!--
  This is the default (empty) long-term memory shipped with iris. Your
  personal memory lives in memory.md at the project root — that file is
  gitignored and will be loaded in preference to this one when present.

  Iris can add facts here automatically by emitting <remember>fact</remember>
  tags mid-reply, and you can edit memory.md directly via the settings panel
  in the browser UI.
-->
